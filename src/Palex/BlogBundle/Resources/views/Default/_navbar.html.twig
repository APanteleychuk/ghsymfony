<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div id="navbar" class="navbar-collapse collapse">
                {% set menu = knp_menu_get('main', [], {'slug': app.request.get("slug")}) %}
                {{ knp_menu_render(menu, { 'currentClass': 'active' }) }}
            <form class="navbar-form navbar-right" role="search" action="{{ path('palex_blog_homepage') }}" method="get">
                <div class="form-group">
                    <input class="form-control" placeholder="Search Posts" name="q" id="search-id" type="search">
                </div>
                <button type="submit" class="btn btn-default">Search</button>
            </form>
        </div>
    </div>
</nav>
